# Production Planner Backend

PHP REST API using MVC structure, with Docker, MariaDB, and PHPMyAdmin.

## Setup

In a terminal run:

```bash
docker compose up
```

If you want to stop the containers, press Ctrl+C.

Or run:

```bash
docker compose down
```
